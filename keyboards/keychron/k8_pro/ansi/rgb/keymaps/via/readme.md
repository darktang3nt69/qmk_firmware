# Keychron K8 Pro

Add custom key to customKeycodes section of via json files.

Include rules.mk with {VIA_ENABLE = yes} in custom keymap folder to make it VIA compatible.

qmk cli is throwing error during flashing. Use qmk toolbox instead.